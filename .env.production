# Temporary: only NON-SECRET, public NEXT_PUBLIC_* values so production builds
# (Vercel) have blog/tag/project content and comments without per-environment
# configuration. These are already exposed in the shipped client/site.
#
# Do NOT add secrets here (GRAPHCMS_TOKEN, SPOTIFY_*). Those belong in the
# Vercel dashboard env vars. Remove this file once everything is configured in
# Vercel.
NEXT_PUBLIC_GRAPHCMS_ENDPOINT=https://ap-southeast-2.cdn.hygraph.com/content/cl153jqjog8tm01zagkyr7x6c/master
NEXT_PUBLIC_UTTERANCES_REPO=phuonganguyen/culi-coder
